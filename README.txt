Cena Data Transfer Agent 

PHP/JavaScript framework for HTML5's local databases 
to synchronize database on client and server. 

Both data as well as relations can be transfered to server 
even if the relation uses auto-numbered primary key. 

Uses PHP5.3, MySQL, and jQuery. 

